export const metadata = {
  title: 'CV Validator',
  description: 'Mini CV Validator',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
